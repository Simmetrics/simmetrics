sudo: false
language: java
jdk:
  - oraclejdk8
  - openjdk7

branches:
  only:
  - master
  - develop
  - release-*

before_install:
 - mvn -T 1C clean

after_success:
  - mvn -T 1C clean cobertura:cobertura coveralls:report -P coveralls.io

